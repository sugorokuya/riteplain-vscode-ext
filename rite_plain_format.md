# RitePlain マークアップ言語仕様書

## 目次

1. [概要](#1-概要)
2. [基本構文](#2-基本構文)
3. [ブロック要素](#3-ブロック要素)
   - [見出し](#31-見出し)
   - [段落](#32-段落)
   - [リスト](#33-リスト)
   - [説明リスト（定義リスト）](#34-説明リスト定義リスト)
   - [リンク](#35-リンク)
   - [画像](#36-画像)
   - [動画](#37-動画)
   - [補足情報](#38-補足情報)
   - [区切り線](#39-区切り線)
4. [インライン要素](#4-インライン要素)
   - [太字](#41-太字)
5. [特殊構文](#5-特殊構文)
   - [コメント](#51-コメント)
   - [行連結](#52-行連結)
6. [制限と制約](#6-制限と制約)
7. [エラー処理](#7-エラー処理)
8. [HTMLレンダリング](#8-htmlレンダリング)
9. [例とベストプラクティス](#9-例とベストプラクティス)

## 1. 概要

RitePlainは、sugorokuya.jpプロジェクトで使用されている軽量マークアップ言語です。AsciiDocに影響を受けた構文を持ち、HTMLに変換して表示されます。シンプルで読みやすい構文でありながら、記事作成に必要な機能を提供することを目的としています。

RitePlainは、Ruby YACC（RACC）パーサーを使用して解析され、HTMLに変換されます。

## 2. 基本構文

RitePlainはフラットなドキュメント構造を採用しています。ドキュメントは以下の要素から構成されます：

- 見出し（Heading）
- 段落（Paragraph）
- リスト（List）
- 説明リスト（Definition List）
- リンク（Link）
- 画像（Image）
- 動画（Video）
- 補足情報（Supplement）
- 区切り線（Thematic Break）

RitePlainで記述された文書は、これらの要素が単純に並べられた形になります。要素間は空行で区切ります。

## 3. ブロック要素

### 3.1 見出し

見出しは`#`記号で始まり、スペースを入れてから見出しテキストを記述します。`#`の数で見出しレベルを示します。

#### 構文

```
# 見出しレベル1
## 見出しレベル2
### 見出しレベル3
```

#### 規則

- 見出しは行頭から`#`で始める
- `#`の後には必ず半角スペースが必要
- 見出しは最大3レベルまでサポート
- 見出しテキスト内では[太字](#41-太字)などの装飾が使用可能

#### 例

```
# ボードゲームの楽しみ方
## 初心者向けゲーム
### [[オススメ]]の2人用ゲーム
```

### 3.2 段落

段落は特別なマークアップなしで記述する連続したテキストです。空行で区切られます。

#### 構文

```
これは段落です。
複数行に渡って書くことができ、行が連続している限り同じ段落として扱われます。

これは新しい段落です。
```

#### 規則

- 行頭に特殊マークアップがない場合、そのテキスト行は段落として扱われる
- 連続した行は同じ段落に含まれる
- 段落は空行で区切られる
- 段落内では[太字](#41-太字)などの装飾が使用可能

### 3.3 リスト

RitePlainはバレット付きリスト（番号なし）と番号付きリストの2種類をサポートしています。

#### 構文

バレット付きリスト（番号なし）:
```
* 項目1
* 項目2
** 項目2-1
** 項目2-2
* 項目3
```

番号付きリスト（番号付き）:
```
. 項目1
. 項目2
.. 項目2-1
.. 項目2-2
. 項目3
```

#### 規則

- リストマーカーには`*`（バレット付き）または`.`（番号付き）を使用
- マーカーの後には半角スペースが必要
- マーカーの連続数（`*`, `**`, `***`など）がリストの階層レベルを表す
- 階層レベルは最大5レベルまでサポート
- 階層が深くなる場合は1段階ずつしか増やせない（`*` → `**`は可能、`*` → `***`は不可）
- 同じリスト内で`*`と`.`のマーカーを混在させることができる
- リストは空行によって区切られる
- リスト項目内では[太字](#41-太字)などの装飾が使用可能

#### エラー処理

- リストの階層が6以上になると、「リストの階層が深すぎます。最大レベルは5です」というエラーメッセージが表示されます
- 階層が2つ以上飛ぶと（例：`*` → `***`）、「階層は1レベルずつ深めなければなりません」というエラーメッセージが表示されます

#### 例

```
* 人気のじっくり型ボードゲーム
** [[カタン]]
*** 拡張セット
** [[ドミニオン]]
** [[パンデミック]]
* もっと複雑なボードゲーム
** [[テラフォーミング・マーズ]]
```

混在 :
```
* 手番で行なうこと
.. アクション
.. 買い物
*** このときに許されるアクションもあります。
... 手札から必要額を出す
... 額内で場から1枚購入
... 捨て場に置く
.. 一掃
.. カード引き
```

### 3.4 説明リスト（定義リスト）

説明リストは、用語とその定義/説明をペアで示すリストです。

#### 構文

```
用語:: 定義または説明
別の用語:: この用語の説明
```

#### 規則

- 用語の後に`::`を続け、その後に半角スペースを入れてから定義を記述
- 複数の定義リスト項目は連続して記述可能
- 定義部分では[太字](#41-太字)などの装飾が使用可能

#### 例

```
ワーカープレイスメント:: プレイヤーが自分の駒（ワーカー）をボード上に配置して行動を選択するメカニズム
トリックテイキング:: カードを出し合い、勝った人がそのカード（トリック）を獲得するメカニズム
エリアコントロール:: ボード上の特定領域の支配権を争うメカニズム
```

### 3.5 リンク

リンクは`link:`で始まるブロック要素で、外部URLやリソースへのリンクを提供します。

#### 構文

```
link:https://example.com[リンクテキスト]
```

または（リンクテキストなし）:
```
link:https://example.com
```

#### 規則

- リンクは行頭から始める必要がある
- `link:`の後にURLを記述し、必要に応じて`[]`内にリンクテキストを指定
- リンクテキストを省略した場合、URLがそのまま表示される

#### 例

```
link:https://boardgamegeek.com[BoardGameGeek]
link:https://example.com/rules.pdf[ルールブック（PDF）]
```

### 3.6 画像

画像は`image::`で始まるブロック要素で、記事内に画像を表示します。

#### 構文

```
image::slot-1[画像キャプション]
```

または（キャプションなし）:
```
image::slot-1
```

オプションのサイズ指定:
```
image::slot-1[画像キャプション, size=small]
```

#### 規則

- 画像は行頭から始める必要がある
- `image::`の後に`slot-番号`形式のスロット識別子を記述
- 必要に応じて`[]`内にキャプションを指定可能
- キャプションの後にカンマと追加属性を指定可能（例：`size=small` または `size=majority`）

#### 例

```
image::slot-1[ゲームの箱絵]
image::slot-2[ゲームボードの配置例, size=majority]
```

### 3.7 動画

動画は`video::`で始まるブロック要素で、YouTube動画などを埋め込み表示します。

#### 構文

```
video::VIDEO_TYPE:VIDEO_ID[動画タイトル]
```

YouTube動画であることを明示的に指定する場合:
```
video::youtube:VIDEO_ID[動画タイトル]
```

または（タイトルなし）:
```
video::VIDEO_TYPE:VIDEO_ID
```

または（VIDEO_TYPEなし = 'YouTube'が既定値）:
```
video::VIDEO_ID
```

#### 規則

- 動画は行頭から始める必要がある
- `video::`の後に、動画タイプ:(任意)、次いで動画IDを記述
- 必要に応じて`[]`内にタイトル指定可能
- 現在はYouTube動画のみサポート（埋め込みは常にYouTubeプレーヤーとして処理）

#### 例

```
video::youtube:dQw4w9WgXcQ[ボードゲーム解説動画]
video::youtube:dQw4w9WgXcQ
video::dQw4w9WgXcQ
```

### 3.8 補足情報

補足情報は`|`（パイプ記号）で始まるブロック要素で、補足的な説明や備考を表示します。

#### 構文

```
| 補足情報テキスト
| 複数行に渡って書くこともできます
| それぞれの行の先頭に | を付けます
```

#### 規則

- 補足情報は行頭から`|`で始める（`|`の後のスペースは任意）
- 複数行の補足情報は、各行の先頭に`|`を付ける
- 補足情報内では[太字](#41-太字)などの装飾が使用可能

#### 例

```
| このゲームは[[2～4人]]でプレイできます
| プレイ時間は約30分です
```

### 3.9 区切り線

区切り線は3つ以上のハイフン（`---`）を使用して、コンテンツ間の視覚的な区切りを表現します。

#### 構文

```
---
```

#### 規則

- 区切り線は行頭から3つ以上のハイフンで記述
- 区切り線の前後には通常、空行が必要

#### 例

```
ここまでがルールの基本部分です。

---

以下はバリアントルールについての説明です。
```

## 4. インライン要素

### 4.1 太字

テキストを強調するために使用します。

#### 構文

```
[[強調したいテキスト]]
```

#### 規則

- テキストを`[[`と`]]`で囲むことで、そのテキストが太字で表示される
- 見出し、段落、リスト項目、説明リスト、補足情報内で使用可能
- 複数の太字要素を同一行内に含めることができる

#### 例

```
このゲームでは[[リソース管理]]と[[エリアコントロール]]の要素が組み合わされています。
```

```
* [[初心者向け]]ボードゲーム
```

## 5. 特殊構文

### 5.1 コメント

コメントは文書変換時に出力されないテキストで、メモやドラフト段階の記述などに使用します。

#### 構文

```
// これはコメントです
```

#### 規則

- コメントは行頭から`//`で始める
- コメントは出力されず、HTML生成時に完全に無視される
- 行の途中で`//`から始まるコメントは、その前に半角スペースが1つ以上ある場合のみコメントとみなされる

#### 例

```
// ここは公開前に要確認
ゲームの推奨プレイ人数は2～4人です。
```

### 5.2 行連結

行連結は行末に`+`を付けることで、文書変換時に改行を挿入せずに次の行と連結します。

#### 構文

```
この行は+
次の行と連結されます。
```

#### 規則

- 行末に`+`を付けると、その行と次の行が連結される
- 行連結では、改行が無視され、連続したテキストとして扱われる

#### 例

```
このゲームのデザイナーはクラウス・トイバー+
で、出版元はコスモスです。
```

出力例: 「このゲームのデザイナーはクラウス・トイバーで、出版元はコスモスです。」

## 6. 制限と制約

RitePlainには、以下の制限と制約があります：

1. **リストのネスト制限**: リストの階層は最大5レベルまで
2. **階層レベルのステップ制限**: リストの階層は一度に1レベルずつしか深くできない
3. **見出しレベルの制限**: 見出しは最大3レベル（`#`, `##`, `###`）まで
4. **ブロックネストの制限**: 基本的にブロック要素のネストは不可（段落内にリストを入れるなど）
5. **行頭要素の制限**: リンク、画像、動画などの特殊要素は行頭からしか始められない

## 7. エラー処理

RitePlainパーサーは、一部の構文エラーを検出し、エラーメッセージを表示します：

1. **リスト階層過剰**: レベル6以上のリストでは「リストの階層が深すぎます。最大レベルは5です」
2. **リスト階層ジャンプ**: 2レベル以上の階層ジャンプでは「階層は1レベルずつ深めなければなりません」
3. **不正なマーカー**: 不正なリストマーカーは通常のテキストとして扱われる
4. **不正な画像スロット**: 不正な画像スロット形式や存在しない画像スロットはエラーメッセージを表示

エラーメッセージはHTMLレンダリング時に表示され、ユーザーに問題を通知します。

## 8. HTMLレンダリング

RitePlainの各要素は、以下のようにHTMLに変換されます：

- **見出し**: `<h1>`, `<h2>`, `<h3>` タグに変換
- **段落**: `<p>` タグに変換
- **リスト**: ネストされた `<ol>` と `<li>` タグに変換（バレット付きリストは `bullet-item` クラスが追加される）
- **説明リスト**: `<dl>`, `<dt>`, `<dd>` タグに変換
- **リンク**: `<a>` タグに変換（ボタンスタイルを適用）
- **画像**: `<figure>` と `<img>` タグに変換（キャプションがある場合は `<figcaption>` も含む）
- **動画**: YouTubeの場合、`<iframe>` を使用した埋め込みに変換
- **補足情報**: `supplement` クラスの `<div>` に変換
- **区切り線**: `<hr>` タグに変換
- **太字**: `<strong>` タグに変換

## 9. 例とベストプラクティス

### 完全な例

以下は、RitePlainのさまざまな機能を使用した完全な例です：

```
# ボードゲーム「カタン」の基本ルール

「カタン」は[[資源管理]]と[[交渉]]が特徴の人気ボードゲームです。
2～4人でプレイでき、プレイ時間は約60分です。

## ゲームの目的

勝利点10点を最初に獲得したプレイヤーが勝者となります。

## ゲームの準備

1. ゲームボードを組み立てる
2. 資源カードを種類ごとに分ける
3. 開発カードをシャッフルする

| プレイヤーは初期配置として2つの開拓地と2つの道を配置します。
| この配置によって序盤の資源生産に大きな差が出るため、慎重に選びましょう。

### 初期配置のコツ

* 様々な数字の隣に配置する
** 特に6と8の隣は資源が出やすい
* 様々な種類の資源を確保する
* 港の近くに配置することも検討する

---

## ゲームの流れ

プレイヤーは順番に以下のアクションを実行します。

. サイコロを振る
.. 出た目の数字のマスから全プレイヤーが資源を得る
. トレードを行う（任意）
.. プレイヤー同士で資源交換
.. 銀行や港と資源交換
. 建設を行う（任意）

image::slot-1[カタンのゲームボード]

詳しいルールについては以下のリンクを参照してください。

link:https://example.com/catan-rules[完全なルールブック（PDF）]

また、ゲームのプレイ方法を解説した動画も参考になります。

video::dQw4w9WgXcQ[カタン基本ルール解説]

## 用語解説

開拓地:: プレイヤーの集落を表すコマで、勝利点1点に相当
都市:: 開拓地をアップグレードしたもので、勝利点2点に相当
道:: 開拓地や都市を繋ぐ線で、領土拡大に必要
```

### ベストプラクティス

1. **構造化**: 適切な見出しレベルを使用して文書を階層化する
2. **空行**: 異なる要素間に空行を入れて読みやすさを向上させる
3. **リスト階層**: リストは適切な階層を守り、深すぎないようにする
4. **太字の活用**: 重要な用語や概念を太字にして強調する
5. **補足情報**: 追加の説明や注釈を補足情報ブロックにまとめる
6. **コメント**: 編集中のメモやドラフト部分にはコメントを活用する
7. **リンクと画像**: 外部リソースへのリンクや視覚的な理解を助ける画像を効果的に配置する
